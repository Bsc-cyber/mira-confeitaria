// Aguarda o carregamento completo do DOM da página Home
document.addEventListener("DOMContentLoaded", function () {
    
    // Log técnico impresso no console (F12) confirmando a inicialização segura do painel
    console.log("Interface clara da Home ativada com sucesso.");

});
